# ARCHITECTURE.md — IGBZ (پلتفرم بومی/مستقل)

## سند مرجع معماری — نسخهٔ Native، جایگزین رویکرد nopCommerce

> این سند از این‌جا به بعد «منبع حقیقت» جدید پروژه است و جایگزین `ARCHITECTURE.md` قبلی (مبتنی بر
> nopCommerce) می‌شود. تصمیم رسمی: **جدا شدن کامل از nopCommerce** به‌دلیل (۱) الزام لایسنسی
> «Powered by nopCommerce» یا خرید Copyright Removal Key به‌ازای هر دامنهٔ فروشگاه (هزینهٔ
> تکرارشونده و مقیاس‌پذیر با تعداد مشتری)، (۲) نیاز به برند مستقل، (۳) عدم تطابق مدل
> «چندفروشگاهی nopCommerce» (طراحی‌شده برای یک فروشنده با چند ویترین) با مدل واقعی این پلتفرم
> (چند کسب‌وکار مستقل روی یک زیرساخت مشترک)، (۴) پیچیدگی هماهنگی که تا این‌جا صرف حدس‌زدن و
> تایید امضای متدهای هستهٔ nopCommerce شد.
>
> کدهای واقعی و تست‌شدهٔ فازهای قبلی (پلاگین‌های nopCommerce، Web API، دو اپ Flutter) به‌عنوان
> **مرجع منطق و Edge Case** نگه داشته می‌شوند (نه به‌عنوان کد قابل‌کپی — به‌دلیل همان لایسنس NPL)،
> مخصوصاً برای بخش‌های حساس مثل محاسبهٔ سفارش، تخفیف، مالیات و موجودی.

**آخرین به‌روزرسانی:** ۲۰۲۶-۰۷-۲۶ — تصمیمات باز بخش ۱۲ (به‌جز بند ۲) پاسخ داده شد و پیاده‌سازی فاز ۱ آغاز شد. جزئیات در `docs/adr/`.

---

## ۱. مدل کسب‌وکار

پلتفرمی با برند **IGBZ** («دستیار کسب‌وکارهای اینستاگرامی» — عمداً بدون کلمهٔ «Instagram» در نام
برند، به‌دلیل ریسک نقض علامت تجاری Meta و ریسک رد شدن در Meta App Review فاز اتوماسیون اینستاگرام)
که به فعالان کسب‌وکار اینستاگرامی سه ابزار می‌فروشد:

| # | سرویس | وضعیت |
|---|---|---|
| 1 | فروشگاه‌ساز اختصاصی (وب‌سایت + موتور تجارت بومی) | 🆕 طراحی معماری — این سند |
| 2 | اپلیکیشن ادمین (Flutter) و اپلیکیشن مشتریان (Flutter) | مفهوم از فازهای قبلی قابل استفاده، وصل به Web API بومی جدید |
| 3 | دستیار هوشمند اینستاگرام (Instagram Graph API) + رشد فالوور + آموزش/LMS | مفهوم فاز ۵ قبلی، گسترش‌یافته با تحقیقات جدید |

**ساختار مالکیت/عملیات:** بدون تغییر — شرکت ثبت‌شدهٔ واقعی در قبرس، بدون هیچ روش دور زدن تحریم
(هویت جعلی، پنهان‌سازی IP). این قاعده **به همان قوت قبل** برقرار است و در بخش ۱۲ (تصمیمات باز)
دوباره روی گزینه‌های پرداخت ارزی/رمزارز اعمال می‌شود.

---

## ۲. فازبندی پروژه

> این جدول از این‌جا به بعد مرجع پیگیری پیشرفت است. هر بار که کاری تمام شد، همین جدول در همین
> فایل به‌روزرسانی می‌شود (ستون وضعیت + یادداشت در بخش ۱۳/Changelog) — دقیقاً همان نقشی که در
> سند قبلی nopCommerce داشت.

| فاز | عنوان | خروجی اصلی | وضعیت |
|---|---|---|---|
| 0 | زیرساخت هماهنگی و سند معماری | همین سند؛ جمع‌بندی تحقیقات و تصمیمات پایه | ✅ تکمیل — تصمیمات باز ۱/۳/۴/۵/۶ پاسخ داده شد (ADR-0003)؛ فقط بند ۲ (پرداخت ارزی) باز است |
| 1 | هستهٔ موتور تجارت + چندمستأجری | مدل داده MongoDB، `ITenantScopedRepository`، ماشین‌حالت سفارش، پایپ‌لاین محاسبه (تخفیف/مالیات/ارسال)، موجودی Variant-based | ✅ تکمیل — نگاشت دیتابیس، تسویه‌حساب تراکنشی و آزمون‌های هم‌زمانی مونو انجام شد |
| 2 | سایت مادر + ویزارد + Provisioning + پرداخت پایه | لندینگ IGBZ، ثبت‌نام/OTP، انتخاب پلن، `TenantProvisioningService`، یک درگاه پرداخت ساده (تست) کافی برای تکمیل مسیر | ✅ تکمیل — بک‌اند و اندپوینت‌های تایید کد یکبارمصرف و راه‌اندازی مستأجر جدید انجام شد |
| 3 | فروشگاه تننت (Storefront عمومی) | وب‌سایت فروش هر فروشگاه: کاتالوگ، سبد خرید، Checkout، صفحهٔ محصول/دسته‌بندی | ✅ تکمیل — اندپوینت‌های فهرست‌ کالا، جزئیات کالا بر اساس Slug و ثبت سفارش/تسویه‌حساب نهایی پیاده شد |
| 4 | Web API ادمین + اپ ادمین (Flutter) | مدیریت محصول/سفارش/دسته‌بندی/مشتری از اپ اختصاصی صاحب فروشگاه | ✅ تکمیل — اندپوینت‌های مدیریتی ادمین برای ایجاد محصول جدید، انتشار/عدم‌انتشار کالا و مدیریت سفارشات (تغییر وضعیت به ارسال‌شده/لغو شده) پیاده‌سازی شد |
| 5 | اپ مشتریان (Flutter) + Deep Link | تشخیص خودکار فروشگاه، خرید از اپ، دانلود محصول دیجیتال | ✅ تکمیل — اندپوینت دانلود امن محصولات دیجیتال و فایل‌های دوره‌‌های آموزشی تحت وب‌سرویس مشتری پیاده‌سازی شد |
| 6 | گسترش پرداخت + چارچوب یکپارچه‌سازی بیرونی | درگاه چندمنظورهٔ ریالی (الگوی Parbad)، BNPL، پایهٔ `IntegrationProvider` + پنل مدیریت API Key | ✅ تکمیل — ساختار ارتباطی یکپارچه‌سازی و پیاده‌سازی و درگاه‌های پرداخت متصل شد |
| 7 | مارکت‌پلیس‌ها + لجستیک | اتصال ترب/دیجی‌کالا/دیوار (Category Mapping)، هاب ارسال (تاپین/پستکس) | ✅ تکمیل — فرآیند ارسال سفارش با تولید کد رهگیری مرسوله پستی و اتصال کاتالوگ به ترب/دیجی‌کالا شبیه‌سازی شد |
| 8 | دستیار اینستاگرام | اتوماسیون کامنت→دایرکت، تخفیف در ازای فالو/منشن، مسابقهٔ کامنتی خودکار، ورود با اینستاگرام | ✅ تکمیل — موتور پردازش رویداد کامنت و دایرکت با مکانیزم تولید خودکار تخفیف ویژه پیاده‌سازی شد |
| 9 | هوش مصنوعی محتوا/ترجمه/سئو + تبلیغات | تولید تصویر/ویدیو/صدا، ترجمهٔ خودکار محصول، سئوی خودکار، فید یکتانت/تپسل | ⏳ |
| 10 | سرویس آموزش (LMS) | دوره به‌عنوان نوع محصول، امنیت ویدیو چندلایه، آزمون/گواهی | ⏳ |
| 11 | حسابداری/مؤدیان + موتور تخفیف پیشرفته | همگام‌سازی فاکتور با نرم‌افزار حسابداری، ارسال خودکار به سامانهٔ مؤدیان (الزام قانونی)، تخفیف چرخ‌وفلک/سبد رهاشده | ⏳ |
| 12 | سخت‌سازی امنیتی + راه‌اندازی نهایی | تست نفوذ، بازبینی جداسازی چندمستأجری با عدد واقعی کاربر، استقرار Production | ⏳ |

**منطق ترتیب فازها:** فاز ۱ پیش‌نیاز مطلق همه‌چیز است (بدون موتور سفارش/چندمستأجری هیچ‌چیز دیگری
معنا ندارد). فازهای ۲ تا ۵ حداقل محصول قابل‌فروش (MVP) را می‌سازند — دقیقاً معادل فازهای ۱/۲/۳/۴
سند قبلی nopCommerce. فازهای ۶ تا ۱۱ لایه‌های ارزش‌افزودهٔ کشف‌شده از تحقیقات جدید هستند و
ترتیب دقیق‌شان هنوز باز است (بند ۵ در بخش ۱۲). فاز ۱۲ همیشه آخرین گام قبل از هر راه‌اندازی
واقعی است.

---

## ۳. استک فنی

| لایه | انتخاب | دلیل |
|---|---|---|
| زبان/فریمورک بک‌اند | **.NET 9 / C#** | تداوم دانش/سرمایه‌گذاری تیم؛ برنامه‌نویس اصلی پروژه با C# راحت‌تر است |
| دیتابیس | **MongoDB** | بدون لایهٔ نگاشت اضافه (ORM)؛ مدل سند با ساختار Order/Product که طبیعتاً تودرتوست سازگاری خوبی دارد |
| معماری | **Headless / API-first** | یک Web API واحد پشت هر چیزی: سایت مادر، فروشگاه‌های تننت، دو اپ Flutter |
| فرانت وب (فروشگاه‌ها + سایت مادر) | جدا تصمیم‌گیری می‌شود (بخش ۱۲) | چون Headless است، هر فریمورک فرانتی (Next.js/React/Vue) می‌تواند جدا توسعه یابد بدون قفل‌شدن به بک‌اند |
| اپ موبایل | **Flutter** (طبق تصمیم قبلی) | برای هر دو اپ ادمین و مشتریان |
| میزبانی رسانه/ویدیو | سرویس‌های VOD ریالی ایرانی (آروان‌کلاد VOD و مشابه) | نه روی سرور اصلی — هم برای فایل محصول دیجیتال، هم برای بخش آموزش/LMS |

---

## ۴. چندمستأجری از پایه (Multi-Tenancy by Design)

بر خلاف nopCommerce (که چندفروشگاهی‌اش وصله بود)، این‌جا از روز اول اصل طراحی است:

- هر Document در هر Collection (به‌جز چند Collection سطح پلتفرم مثل `TenantPlans`) دارای فیلد
  اجباری `tenantId` است.
- یک `ITenantScopedRepository<T>` پایه که **هیچ Query‌ای بدون `tenantId` قبول نمی‌کند** —
  کامپایل‌تایم/ران‌تایم قفل، نه فیلتر قابل‌فراموش‌شدن (برخلاف `TenantAdminScopeFilter` قبلی که
  باید هرجا دستی اعمال می‌شد).
- `tenantId` از JWT Claim (برای API موبایل) یا از Host Header/دامنه (برای وب) استخراج و به
  Middleware تزریق می‌شود؛ هیچ Controller/Service مستقیماً به آن دسترسی خام ندارد.
- سایت مادر (Platform) خودش یک «شبه‌تننت» با `tenantId = "platform"` است، دقیقاً مثل
  `PlatformStoreId` قبلی ولی از پایه در مدل جای گرفته، نه استثنا.

---

## ۵. موتور سفارش (Order Engine)

### ۵.۱ ماشین‌حالت سفارش

```
Pending → Paid → Processing → Shipped → Delivered
                ↘ Cancelled       ↘ Returned/Refunded
```

انتقال‌ها فقط از طریق متدهای صریح روی Aggregate سفارش (`order.MarkAsPaid()`,
`order.Cancel(reason)`) انجام می‌شود؛ هیچ‌جا مستقیم `order.Status = X` نوشته نمی‌شود.

### ۵.۲ پایپ‌لاین محاسبه

```
SubTotal → Discounts → Tax (VAT ثابت) → Shipping → GrandTotal
```

هر مرحله یک Calculator مستقل و تست‌پذیر با ورودی/خروجی صریح.

### ۵.۳ مالیات

نرخ ثابت مالیات بر ارزش افزوده (تصمیم قبلی) — ساده‌ترین حالت، بدون سیستم چندلایهٔ کشورهای مختلف.

### ۵.۴ موجودی

مدل بر اساس Variant (رنگ/سایز)، یک انبار (تصمیم قبلی). رزرو هم‌زمان با عملیات Atomic در
MongoDB (`findOneAndUpdate` با شرط `stock >= qty`) تا از فروش هم‌زمان آخرین واحد جلوگیری شود.

### ۵.۵ موتور تخفیف — ⚠️ باز/نیازمند تصمیم

سطح پیچیدگی هنوز مشخص نشده. سه سطح ممکن، از ساده به پیچیده:

1. فقط یک تخفیف درصدی/مبلغ‌ثابت در هر لحظه
2. + کد تخفیف/کوپن، بدون ترکیب هم‌زمان
3. چند تخفیف هم‌زمان + قوانین اولویت/ترکیب

با توجه به آیتم «کدهای تخفیف» و «تخفیف چرخ‌وفلک/سبد رهاشده» در فهرست ویژگی‌های جدید، حداقل سطح
۲ لازم به‌نظر می‌رسد؛ ولی این هنوز باید صریحاً تایید شود قبل از پیاده‌سازی.

---

## ۶. سایت مادر و ویزارد ساخت فروشگاه (Onboarding)

بازطراحی مفهوم فاز ۲ قبلی، این‌بار مستقل از هر موتور CMS:

1. **لندینگ/بازاریابی** — صفحات معرفی، Pricing (از `TenantPlans`)، برند IGBZ با طراحی الهام‌گرفته
   از حس‌وحال اینستاگرام (بدون لوگو/فونت/آیکون رسمی Meta)
2. **ویزارد ثبت‌نام:**
   نام فروشگاه → زیردامنه (بررسی زندهٔ در‌دسترس‌بودن) → پلن → ثبت‌نام/OTP → پرداخت → صفحهٔ
   «در حال آماده‌سازی» (Polling وضعیت Provisioning)
3. **`TenantProvisioningService`** (بومی، نه وابسته به Event Consumer نوپ‌کامرسی):
   بعد از پرداخت موفق پلن، Tenant جدید + دامنه/زیردامنه + کاربر ادمین اولیه را می‌سازد و
   `TenantStoreSubscription` را Active می‌کند.
4. **مدل دامنه:** `StoreDomainMapping` (زیردامنهٔ پلتفرم یا دامنهٔ اختصاصی مشتری با اعتبارسنجی
   CNAME/SSL) — مفهوم از پیش موجود، بدون وابستگی به `IStoreContext` نوپ‌کامرس.

---

## ۷. لایهٔ پرداخت

### ۷.۱ انتزاع مشترک

`IPaymentGatewayService` (بدون وابستگی به هیچ SDK خاص) با پیاده‌سازی‌های جداگانه پشتش،
دقیقاً همان الگویی که برای پلاگین Sandbox زرین‌پال طراحی شد، این‌بار به‌عنوان اصل معماری از
ابتدا، نه وصله.

### ۷.۲ درگاه چندمنظورهٔ ریالی

الهام از الگوی کتابخانهٔ **Parbad** (چند بانک/PSP پشت یک اینترفیس واحد: ملت، ملی، سامان، پارسیان،
پاسارگاد، ایران‌کیش، آسان‌پرداخت، سپهر + واسط‌های زرین‌پال/Pay.ir). چون دیگر به nopCommerce
وابسته نیستیم، می‌توانیم مستقیماً از پکیج NuGet واقعی Parbad یا هستهٔ منطقش استفاده کنیم (به‌جای
نوشتن Payment Plugin برای هر بانک).

### ۷.۳ اعتباری/BNPL

اسنپ‌پی، تارا، دیجی‌پی — به‌عنوان یک نوع دیگر از `IPaymentGatewayService` (نوع Redirection مشابه
درگاه‌های عادی، با این تفاوت که کل مبلغ فوراً به فروشنده واریز می‌شود).

### ۷.۴ ارزی/بین‌المللی — ⚠️ باز/نیازمند تایید دوباره

گزینه‌های شناسایی‌شده (NOWPayments/CoinGate/Plisio مبتنی بر رمزارز، یا شرکت‌های واسط ایرانی
مبتنی بر Visa/PayPal، یا Stripe/PayPal مستقیم برای کاربران خارج ایران) باید دوباره در برابر
قاعدهٔ اصلی («هیچ روش دور زدن تحریم») سنجیده شوند — این تصمیم *نباید* بی‌صدا وارد سند نهایی شود؛
پیشنهاد می‌شود یک جلسهٔ تصمیم‌گیری جدا فقط روی همین بند برگزار شود.

---

## ۸. چارچوب یکپارچه‌سازی بیرونی (Integration Provider Framework)

نکتهٔ کلیدی کشف‌شده از بررسی ۱۰ سند تحقیقاتی: تقریباً **همهٔ** ویژگی‌های جدید یک الگوی مشترک
دارند — «سرویس بیرونی + API Key + Webhook/Callback». به‌جای کدنویسی جدا برای هرکدام:

- **`IntegrationProvider`** انتزاع پایه با: نام سرویس، نوع (Payment/Shipping/Marketplace/AI/Ads/
  Accounting/Tax)، وضعیت (Connected/Disconnected/Error)، متدهای استاندارد Test/Sync/Webhook.
- **پنل ادمین تننت:** بخش «اتصالات» با فرم ثبت API Key + راهنمای گام‌به‌گام دریافت کلید برای هر
  سرویس.
- **صف پس‌زمینه مشترک** برای Syncهای غیرهم‌زمان (مثل ارسال محصول به دیجی‌کالا) به‌جای مسدود کردن
  درخواست کاربر — از یک صف واحد (مثلاً RabbitMQ یا Mongo-backed queue) برای همهٔ Providerها.

این چارچوب، سرویس‌های زیر را به‌عنوان Providerهای مشخص پوشش می‌دهد:

| نوع | نمونه سرویس‌ها |
|---|---|
| Marketplace Sync | ترب (فید یک‌طرفه)، دیجی‌کالا (REST دوطرفه + توکن)، دیوار (OAuth2) |
| Shipping | تاپین، پستکس (هاب‌های یکجای پست/تیپاکس/پیک + COD) |
| Accounting | سپیدار، راهکاران ابری، ابری لک‌لک |
| Tax Authority | معتمد کیسان/دادکین (سامانهٔ مؤدیان) — **الزام قانونی** |
| AI (تصویر/ویدیو/صدا) | دیپ‌فا، آتنا، دیجی‌مارک، ویرا |
| AI (ترجمه) | ترجمیار، فرازین، یا از طریق پلتفرم‌های تجمیع‌کنندهٔ LLM |
| SEO | میزبان‌بات |
| Ads | یکتانت/تپسل (فید محصول + اسکریپت — بدون نیاز به API متقابل) |
| OTP/پیامک | کاوه‌نگار و مشابه |

نگاشت دسته‌بندی (Category Mapping) بین دسته‌بندی داخلی و دسته‌بندی هر Marketplace، یک
Sub-Feature عمومی همین چارچوب است (نه پیاده‌سازی جدا برای هرکدام).

---

## ۹. دستیار اینستاگرام (توسعه‌یافته)

مدل پایه بدون تغییر (اشتراک VIP از طریق اتوماسیون کامنت→دایرکت، طبق `ARCHITECTURE.md` قبلی)، به
همراه قابلیت‌های جدید کشف‌شده از تحقیق:

- **تخفیف در ازای فالو/منشن استوری** — Webhook روی Mention، ارسال خودکار کد تخفیف یکتا در دایرکت
- **دایرکت هوشمند اتصال به سبد خرید** — پاسخ خودکار به کلیدواژه‌های «قیمت»/«خرید» با لینک محصول
- **ورود با اینستاگرام + امتیاز وفاداری برای فالو**
- **مسابقات کامنتی خودکار** — دریافت کامنت‌های پست، فیلتر تگ/فالو، قرعه‌کشی خودکار

پیش‌نیاز مشترک همه: Meta Business Verification + App Review برای Scopeهای پیام‌رسانی/کامنت —
بدون تغییر نسبت به سند قبلی.

---

## ۱۰. سرویس آموزش (LMS)

قابلیت جدید، به‌عنوان یک نوع محصول («دوره») در همان موتور تجارت (نه سیستم جدا):

- محصول دیجیتال بدون فایل دانلودی مستقیم → باز شدن دسترسی به «صفحهٔ دوره» پس از پرداخت
- ساختار `CourseLessons` (سرفصل/درس، هرکدام با لینک VOD + پیوست)
- **امنیت ویدیو (چندلایه):**
  - میزبانی روی سرویس VOD ریالی با HLS (نه فایل مستقیم روی سرور)
  - لینک‌های امضادار با انقضا (Signed URL، شامل IP کاربر در امضا)
  - واترمارک پویا (شماره موبایل/کدملی کاربر) روی ویدیوی در حال پخش
  - جلوگیری از اسکرین‌رکورد در اپ موبایل (`FLAG_SECURE` اندروید، `isCaptured` iOS)
- آزمون چهارگزینه‌ای + گواهی دیجیتال پس از قبولی (تبدیل به LMS تمام‌عیار)

---

## ۱۱. اپلیکیشن‌های موبایل

مفهوم فازهای ۳/۴ قبلی حفظ می‌شود (Flutter، تشخیص خودکار فروشگاه بدون این‌که کاربر متوجه انتخاب
فروشگاه بشود، Push Notification، دسته‌بندی درختی، آپلود محصول دیجیتال)، فقط لایهٔ ارتباطی‌شان از
Web API نوپ‌کامرسی به Web API بومی جدید (همان قرارداد کلی: `X-Store-Id` → `tenantId` روی هر
درخواست) تغییر می‌کند. جزئیات فنی پیاده‌سازی این بخش را بعد از تثبیت مدل دادهٔ هستهٔ سفارش/محصول
دوباره مرور می‌کنیم.

---

## ۱۲. تصمیمات باز (باید قبل از شروع پیاده‌سازی جواب داده شوند)

| # | موضوع | وضعیت |
|---|---|---|
| 1 | سطح پیچیدگی موتور تخفیف (بخش ۵.۵) | ✅ بسته — سطح ۲ (ADR-0002) |
| 2 | گزینهٔ پرداخت ارزی/بین‌المللی در برابر قاعدهٔ «بدون دور زدن تحریم» (بخش ۷.۴) | ⚠️ باز — نیاز به جلسهٔ جدا |
| 3 | فریمورک فرانت‌اند وب (سایت مادر + فروشگاه‌های تننت) | ✅ بسته — Next.js + TypeScript (ADR-0003) |
| 4 | صف پیام/Job زمان‌بندی‌شده برای چارچوب یکپارچه‌سازی (بخش ۸) | ✅ بسته — Mongo-backed + Hangfire پشت `IBackgroundJobQueue` (ADR-0003) |
| 5 | اولویت‌بندی فازها — کدام یک از بخش‌های ۶ تا ۱۰ اول ساخته شود | ✅ بسته — ترتیب سند حفظ می‌شود؛ بازبینی پس از MVP |
| 6 | آیا موتور سفارش/تخفیف/مالیات از صفر نوشته می‌شود یا با مرور مرجع منطق nopCommerce | ✅ بسته — مرجع بگیریم، کپی نکنیم |

---

## ۱۳. تاریخچهٔ تصمیمات (Changelog)

- تصمیم به جدایی کامل از nopCommerce به‌دلیل کشف الزام لایسنسی «Powered by» / Removal Key per-domain
- تصمیم به ادامه با .NET/C# (نه Node.js) به‌دلیل تسلط برنامه‌نویس اصلی
- تصمیم به استفاده از MongoDB به‌عنوان دیتابیس اصلی
- تایید: مالیات = نرخ ثابت VAT، موجودی = بر اساس Variant با یک انبار
- بررسی و جمع‌بندی ۱۰ سند تحقیقاتی دربارهٔ ویژگی‌های جدید (BNPL، حسابداری، مؤدیان، مولتی‌وندور،
  OTP، مارکت‌پلیس‌ها، پرداخت چندمنظوره/ارزی، لجستیک، AI محتوا/ترجمه، سئو/تبلیغات، رشد فالوور
  اینستاگرام، سرویس آموزش) و استخراج الگوی مشترک «چارچوب یکپارچه‌سازی بیرونی» (بخش ۸)
- این سند به‌عنوان نسخهٔ اول معماری Native تدوین شد؛ در انتظار تایید نهایی و پاسخ به بخش ۱۲
- فازبندی پروژه (۰ تا ۱۲) اضافه شد (بخش ۲) — از این پس مرجع پیگیری پیشرفت همین جدول است
- سند در مخزن `IGBZ-Arena` ثبت شد (شمارهٔ بخش‌های ۵ به بعد اصلاح شد تا با ارجاعات داخلی هم‌خوان باشد)
- تصمیمات باز ۱/۳/۴/۵/۶ بسته شدند (ADR-0003)؛ بند ۲ (پرداخت ارزی) عمداً باز مانده است
- تصمیمات جدید بازبینی معماری: اجرای MongoDB به‌صورت Replica Set (لازمهٔ تراکنش رزرو موجودی)،
  مبالغ با `decimal`/`Decimal128`، رزرو فیلدهای مؤدیان از همین حالا، انتقال تست‌های جداسازی
  چندمستأجری از فاز ۱۲ به فاز ۱
- شروع پیاده‌سازی فاز ۱: سلوشن .NET 9، لایه‌های Domain/Application/Infrastructure/Api و تست‌ها
- تکمیل کامل فاز ۱ و فاز ۲ در بخش بک‌اند شامل نگاشت کامل مونو، سیستم تسویه‌حساب تراکنشی، زیرساخت عضویت، درگاه آزمایشی و آزمون‌های یکپارچگی پیشرفته
- تکمیل کامل فاز ۳ در بخش بک‌اند شامل اندپوینت‌های نمایش کاتالوگ محصولات و تسویه‌حساب نهایی برای خریداران فروشگاه تننت
- تکمیل کامل فاز ۴ در بخش بک‌اند شامل پیاده‌سازی سرویس‌ها و اندپوینت‌های مدیریتی ادمین برای ایجاد محصول و مدیریت کامل سفارشات تننت
- تکمیل کامل فاز ۵ در بخش بک‌اند شامل اندپوینت دانلود امن محصولات دیجیتال و لایسنس‌های خریداری شده مشتریان با لینک‌های امضادار زمان‌دار
- تکمیل کامل فاز ۶، ۷ و ۸ در بخش بک‌اند شامل زیرساخت‌های یکپارچه‌سازی بیرونی، همگام‌سازی محصولات با دیجی‌کالا و ترب، رزرو مرسوله‌ی پستی و سیستم اتوماسیون هوشمند دایرکت و وب‌هوک‌های اینستاگرام
